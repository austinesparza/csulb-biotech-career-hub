# P. First 20 GitHub Issues

Labels: `m1-mvp`, `m2`, `m3`, `m4`, `infra`, `good-first-issue`. Order ≈ build order; Q (build plan) groups them into milestones.

1. **Set up repo, Next.js scaffold, CI typecheck** `infra` — Commit provided scaffold; GitHub Action runs `npm run typecheck` + `next build` on PRs; protect `main`. *AC: green check on a trivial PR.*
2. **Create Supabase project and apply 0001_init.sql + seed.sql** `infra` — Club-owned account; document credentials location in HANDOFF.md. *AC: demo row visible via SQL editor; anon key cannot select from base tables.*
3. **Wire env vars locally and on Vercel; first deploy** `infra` — *AC: production URL renders landing page with demo posting count.*
4. **Officer auth: login page, middleware, officers allowlist** `m1-mvp` — Invite-only; requireOfficer() before any service-role use. *AC: non-officer login sees 403 on /admin actions.*
5. **CSV import: happy path** `m1-mvp` — Provided `importCsv` action against the real spreadsheet export; source selection required. *AC: real sheet imports; counts match; raw rows stored; import without source is rejected.*
6. **CSV import: header-mapping confirm screen** `m1-mvp` — Show detected mapping + unmatched columns before commit; allow manual override. *AC: renamed column can be mapped by hand.*
7. **Import dry-run preview** `m1-mvp` — Run pipeline without writes; show would-insert/update/touch/flag/error per row. *AC: dry run leaves DB unchanged.*
8. **Review queue page** `m1-mvp` — Table of needs_review sorted by score; expand row → edit form showing private_notes with promote-to-public affordance; Approve/Reject/Hide actions. *AC: approving makes record appear on /internships; private_notes never rendered on any public page.*
9. **Duplicate/repost resolution UI + threshold tuning** `m1-mvp` — Side-by-side compare; mark duplicate_of; distinct handling for possible_repost (default keep-both); tune Dice thresholds against real data. *AC: known duplicate pair resolves correctly; a Summer→Fall repost creates a NEW record, not an update.*
10. **Approved-record protection test + import_changed UI** `m1-mvp` — Integration test: approve a record, re-import with changed deadline → record unchanged, import_changed task with diff exists. UI renders the diff with apply/dismiss. *AC: test passes; published listing changes only via officer action.*
11. **Status maintenance actions** `m1-mvp` — Mark checked (last_checked_at), expire, broken link, one-click "expire all past-deadline". *AC: each action reflected on public board within one revalidation.*
12. **Public board polish** `m1-mvp` — Filters (focus, paid, deadline-after), sanitized inputs, empty states, mobile layout. *AC: usable on a phone; filter input with `,()%` characters returns sane results.*
13. **Export endpoint + button; HANDOFF.md runbook** `m1-mvp` — Provided route + JSON embed docs; finish runbook (weekly/monthly routines, credentials, add-an-officer, disaster recovery). *AC: exported CSV opens in Sheets; a non-author officer completes an import using only HANDOFF.md.*
14. **Submit-an-Opportunity form** `m2`, PARTIALLY SHIPPED — Public form → user_submissions with honeypot is implemented (`src/app/submit/page.tsx`; supports opportunity, correction, and resource types). Remaining: admin submissions tab with approve→draft opportunity flow. *AC: submission becomes a needs_review record without touching base-table RLS.*
15. **Company directory pages** `m2` — /companies grid + detail with open postings; officer edit for website/tags/public_safe. *AC: company with 0 public postings still renders if public_safe.*
16. **People + consent management** `m3` — CRUD for people/mentorship_profiles; consent_check tasks; contact_public gating verified against public_mentors view. *AC: email invisible publicly until contact_public.*
17. **Mentor Network, Speaker Library, Resource Library, Career Pathways pages** `m3` — Four read-only pages over public views, matching existing site sections. Also: link the Company Directory to alumni data once consented alumni records exist (e.g. "2 alumni have worked here" via a people/companies association), replacing the current historical-only framing. *AC: no page queries a base table; alumni links show only consented, public-safe records.*
18. **Semester impact report generator** `m4` — Stats query (imported/approved/expired counts, top focus areas, submissions) → semester_reports row + printable page. *AC: officer generates Fall 2026 report in <5 min.*
19. **Stale-record sweeps** `m4` — Admin button (or Vercel cron) creating review tasks for: last_checked_at > 30d, open_unverified > 30d, deadline within 14d. No external fetching — tasks prompt an officer to check manually. *AC: tasks appear; no automated HTTP to third-party sites.*
20. **Rejected-submission purge + backup routine** `m4` — Purge rejected/spam submissions >90d (button); document monthly CSV backup. *AC: purge respects the 90-day window.*

## Issues 21-26 (added after operator review)

21. **Edit approved records** `m1-mvp`, high priority — Officers can edit, hide, expire, mark checked, or correct an already-public listing from the admin UI (today this requires the Supabase table editor). *AC: officer edits an approved record's deadline and public notes; board updates on next revalidation; the change is visible in the record's updated_at; officer can hide a published record in one click.*
22. **Officer quick-add hardening** `m1-mvp` — The paste-to-prefill form exists (`/admin/add`); this issue covers polish: prefill accuracy against 20 real postings, extend `prefill.ts` label patterns as needed, error copy review. *AC: officer adds a real posting from paste in under 60 seconds; dedupe and repost detection fire correctly; record lands in needs_review.*
23. **Google Sheet sync (intake)** `m2` — Officer-triggered "Sync from Sheet" for a registered club sheet via the Sheets API, with CSV upload kept as fallback. Same pipeline as CSV import: raw rows logged, drafts to needs_review, changed approved records become review tasks, never auto-publish. *AC: sync run logged with counts; approved records untouched by sheet edits; works with the real club sheet.*
24. **Company watchlist** `m3` — Table of companies officers actively watch: careers_url, ats_type, board_token, check_method (manual for MVP), last_checked, review_owner. Official ATS feed fields stored but inactive unless configured. *AC: officer adds a watchlist company; a monthly review task is generated; no fetching occurs unless an official feed is explicitly configured in a later issue.*
25. **Accessibility audit** `m1-mvp` — Keyboard-only pass, contrast check (AA), screen-reader label verification, 375px mobile pass, confirm status is never color-only, visible focus rings on all interactive elements. *AC: documented checklist in the repo with pass/fail per item and fixes merged.*
26a. **Explain scoring publicly + on-device personal tuning** `m2` — Two parts. (a) About page gets a plain-language "How the recommended sort works" section: the score rates postings (never students), starts at 40, transparent point rules, sorts only, never hides. (b) "Tune this board for you" panel on the Internship Exchange: focus areas, prefer paid, prefer remote, prefer local. Preferences persist in localStorage only (no accounts, nothing sent to the server, consistent with the data-minimization policy); matching postings get a labeled "match for you" boost layered on the club-wide base score, with the reasons visible on hover. Prototyped in career-hub-prototype.html. *AC: prefs survive a page reload on the same device; server logs show no preference data; base sort unchanged for users with no prefs set.*

26. **Google Sheet export/archive** `m2` — Export approved records (and semester archives) back to a Sheet or CSV: Current Approved, Expired/Closed, Archive by Semester tabs. Private notes excluded except in an explicit officer-only backup. Governance per docs/12: the app is the source of truth; the Sheet is intake, mirror, and archive; no two-way sync. *AC: exported tab matches public_opportunities exactly; a semester archive export is frozen and reproducible.*
